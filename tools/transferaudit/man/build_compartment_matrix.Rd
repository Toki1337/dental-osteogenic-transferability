% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transferaudit.R
\name{build_compartment_matrix}
\alias{build_compartment_matrix}
\title{Build a genes x compartments standardized-mean expression matrix}
\usage{
build_compartment_matrix(expr, compartment)
}
\arguments{
\item{expr}{genes x cells matrix (base matrix or Matrix::dgCMatrix), log-normalized.}

\item{compartment}{character/factor vector of length ncol(expr): cell compartment labels.}
}
\value{
matrix M (genes x compartments) of standardized mean expression; genes
  with zero variance are dropped.
}
\description{
The per-compartment mean of a per-cell z-scored signature score equals the
signature-averaged per-compartment mean of each gene's z-scored expression.
This function precomputes that small genes x compartments matrix `M` from a
(sparse) log-normalized expression matrix, so signature scoring and the
permutation null reduce to fast row-subset column means.
}
